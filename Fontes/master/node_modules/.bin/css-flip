#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../css-flip/bin/css-flip" "$@"
  ret=$?
else 
  node  "$basedir/../css-flip/bin/css-flip" "$@"
  ret=$?
fi
exit $ret
